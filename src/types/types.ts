export interface MenuItem {
    title: string;
    url: string;
    icon: string;
}

export interface CardItem {
    title: string;
    url: string;
    description: string;
}

export interface faq {
    question: string;
    answer: string;
}
